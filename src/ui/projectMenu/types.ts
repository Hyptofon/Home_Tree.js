/** Construction options for {@link ProjectMenuPanel}. */
export interface ProjectMenuPanelOptions {
  /** HUD overlay elements affected by the menu's visibility toggle. */
  readonly hudTargets: readonly HTMLElement[];
}
